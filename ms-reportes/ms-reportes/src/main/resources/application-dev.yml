spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_reportes?useSSL=false&serverTimezone=UTC
    username: root
    password:

eureka:
  client:
    service-url:
      defltZone: http://localhost:8761/eureka/