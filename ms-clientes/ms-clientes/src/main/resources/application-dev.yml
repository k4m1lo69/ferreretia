spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_clientes?useSSL=false&serverTimezone=UTC
    username: root
    password:

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/